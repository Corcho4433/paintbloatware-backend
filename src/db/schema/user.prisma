model User {
  id       String    @id @default(cuid())
  name     String
  email    String    @unique
  password String
  comments Comment[]
  posts    Post[]
  ratings  Ratings[]
  sessions Session[]

  @@map("users")
}
